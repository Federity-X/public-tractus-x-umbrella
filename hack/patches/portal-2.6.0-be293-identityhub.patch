--- a/templates/cronjob-backend-processes.yaml
+++ b/templates/cronjob-backend-processes.yaml
@@ -132,6 +132,10 @@
               value: "{{ .Values.portalAddress }}{{ .Values.backend.portalIntroductionDataspacePath }}"
             - name: "APPLICATIONACTIVATION__USEDIMWALLET"
               value: "{{ .Values.backend.useDimWallet }}"
+            {{- if .Values.backend.walletProvider }}
+            - name: "ONBOARDING__WALLETPROVIDER"
+              value: "{{ .Values.backend.walletProvider }}"
+            {{- end }}
             - name: "APPLICATIONCHECKLIST__BPDM__BASEADDRESS"
               value: "{{ .Values.bpdm.portalGateAddress }}{{ .Values.bpdm.portalGateApiPath }}"
             - name: "APPLICATIONCHECKLIST__BPDM__CLIENTID"
@@ -325,6 +333,33 @@
               value: "{{ .Values.backend.processesworker.dim.encryptionConfigs.index0.cipherMode }}"
             - name: "APPLICATIONCHECKLIST__ISSUERCOMPONENT__ENCRYPTIONCONFIGS__0__PADDINGMODE"
               value: "{{ .Values.backend.processesworker.dim.encryptionConfigs.index0.paddingMode }}"
+            {{- if .Values.backend.processesworker.identityHub.enabled }}
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__BASEADDRESS"
+              value: "{{ .Values.backend.processesworker.identityHub.baseAddress }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__APIKEY"
+              valueFrom:
+                secretKeyRef:
+                  name: "{{ .Values.backend.interfaces.secret }}"
+                  key: "identityhub-api-key"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__DIDDOCUMENTBASELOCATION"
+              value: "{{ .Values.backend.processesworker.identityHub.didDocumentBaseLocation }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__CREDENTIALSERVICEBASEADDRESS"
+              value: "{{ .Values.backend.processesworker.identityHub.credentialServiceBaseAddress }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__ISSUERDID"
+              value: "{{ .Values.backend.processesworker.identityHub.issuerDid }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__BPNCREDENTIALDEFINITIONID"
+              value: "{{ .Values.backend.processesworker.identityHub.bpnCredentialDefinitionId }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__MEMBERSHIPCREDENTIALDEFINITIONID"
+              value: "{{ .Values.backend.processesworker.identityHub.membershipCredentialDefinitionId }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__ISSUERADMINBASEADDRESS"
+              value: "{{ .Values.backend.processesworker.identityHub.issuerAdminBaseAddress }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__ISSUERADMINAPIKEY"
+              value: "{{ .Values.backend.processesworker.identityHub.issuerAdminApiKey }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__ISSUERPARTICIPANTID"
+              value: "{{ .Values.backend.processesworker.identityHub.issuerParticipantId }}"
+            - name: "APPLICATIONCHECKLIST__IDENTITYHUB__FRAMEWORKCONTRACTVERSION"
+              value: "{{ .Values.backend.processesworker.identityHub.frameworkContractVersion }}"
+            {{- end }}
             - name: "BPNDIDRESOLVER__BASEADDRESS"
               value: "{{ .Values.bpnDidResolver.managementApiAddress }}"
             - name: "BPNDIDRESOLVER__APIKEY"
--- a/templates/deployment-backend-administration.yaml
+++ b/templates/deployment-backend-administration.yaml
@@ -482,6 +486,10 @@
           value: "{{ .Values.portalAddress }}{{ .Values.backend.portalHelpPath }}"
         - name: "REGISTRATION__USEDIMWALLET"
           value: "{{ .Values.backend.useDimWallet }}"
+        {{- if .Values.backend.walletProvider }}
+        - name: "ONBOARDING__WALLETPROVIDER"
+          value: "{{ .Values.backend.walletProvider }}"
+        {{- end }}
         - name: "SERVICEACCOUNT__CLIENTID"
           value: "{{ .Values.backend.administration.serviceAccount.clientId }}"
         - name: "SERVICEACCOUNT__ENCRYPTIONCONFIGINDEX"
--- a/templates/deployment-backend-registration.yaml
+++ b/templates/deployment-backend-registration.yaml
@@ -218,6 +218,10 @@
           value: "{{ .Values.backend.processesworker.mailing.encryptionConfigs.index0.paddingMode }}"
         - name: "APPLICATIONCREATION__USEDIMWALLET"
           value: "{{ .Values.backend.useDimWallet }}"
+        {{- if .Values.backend.walletProvider }}
+        - name: "ONBOARDING__WALLETPROVIDER"
+          value: "{{ .Values.backend.walletProvider }}"
+        {{- end }}
         ports:
         - name: http
           containerPort: {{ .Values.portContainer }}
--- a/templates/secret-backend-interfaces.yaml
+++ b/templates/secret-backend-interfaces.yaml
@@ -48,6 +48,7 @@
   issuercomponent-client-secret: {{ coalesce ( .Values.backend.processesworker.issuerComponent.clientSecret | b64enc ) ( index $secret.data "issuercomponent-client-secret" ) | default ( randAlphaNum 32 ) | quote }}
   dim-encryption-key0: {{ coalesce ( .Values.backend.processesworker.dim.encryptionConfigs.index0.encryptionKey | b64enc ) ( index $secret.data "dim-encryption-key0" ) | default ( randAlphaNum 32 ) | quote }}
   bpndidresolver-api-key: {{ coalesce ( .Values.backend.processesworker.bpnDidResolver.apiKey | b64enc ) ( index $secret.data "bpndidresolver-api-key" ) | default ( randAlphaNum 32 ) | quote }}
+  identityhub-api-key: {{ coalesce ( .Values.backend.processesworker.identityHub.apiKey | b64enc ) ( index $secret.data "identityhub-api-key" ) | default ( randAlphaNum 32 ) | quote }}
   serviceaccount-encryption-key0: {{ coalesce ( .Values.backend.administration.serviceAccount.encryptionConfigs.index0.encryptionKey | b64enc ) ( index $secret.data "serviceaccount-encryption-key0" ) | default ( randAlphaNum 32 ) | quote }}
   offerprovider-encryption-key0: {{ coalesce ( .Values.backend.processesworker.offerprovider.encryptionConfigs.index0.encryptionKey | b64enc ) ( index $secret.data "offerprovider-encryption-key0" ) | default ( randAlphaNum 32 ) | quote }}
 {{ else -}}
@@ -69,6 +70,7 @@
   issuercomponent-client-secret: {{ .Values.backend.processesworker.issuerComponent.clientSecret | default ( randAlphaNum 32 ) | quote }}
   dim-encryption-key0: {{ .Values.backend.processesworker.dim.encryptionConfigs.index0.encryptionKey | default ( randAlphaNum 32 ) | quote }}
   bpndidresolver-api-key: {{ .Values.backend.processesworker.bpnDidResolver.apiKey | default ( randAlphaNum 32 ) | quote }}
+  identityhub-api-key: {{ .Values.backend.processesworker.identityHub.apiKey | default ( randAlphaNum 32 ) | quote }}
   serviceaccount-encryption-key0: {{ .Values.backend.administration.serviceAccount.encryptionConfigs.index0.encryptionKey | default ( randAlphaNum 32 ) | quote }}
   offerprovider-encryption-key0: {{ .Values.backend.processesworker.offerprovider.encryptionConfigs.index0.encryptionKey | default ( randAlphaNum 32 ) | quote }}
 {{ end }}
--- a/values.yaml
+++ b/values.yaml
@@ -307,6 +307,10 @@
   userManagementPath: "/usermanagement"
   # -- Enable for using Decentral Identity Management (DIM) instead of Managed Identity Wallet (MIW) as wallet
   useDimWallet: false
+  # -- BE-293: deployment-wide onboarding wallet provider. Empty (default) preserves the historical
+  # DIM/Custodian behaviour (driven by useDimWallet); "IdentityHub" selects the IdentityHub wallet +
+  # holder credential-request issuer path. Set together with backend.processesworker.identityHub.*.
+  walletProvider: ""
   keycloak:
     # -- Secret containing the database-password and the client-secret for the connection to the centralidp (CX IAM) and the client-secret for the connection to the sharedidp (CX-IAM).
     secret: "portal-backend-keycloak"
@@ -1005,6 +1009,23 @@
       clientSecret: ""
       grantType: "client_credentials"
       scope: "openid"
+    # -- IdentityHub onboarding wallet (BE-293). Only rendered/validated when 'enabled' is true, so
+    # stub/DIM deployments are unaffected. Enable together with backend.walletProvider=IdentityHub.
+    identityHub:
+      enabled: false
+      # -- Base address of the IdentityHub Identity API (admin), e.g. http://identity-hub.tx.test/api/identity
+      baseAddress: ""
+      # -- did:web base location the holder DID is built from (did:web:{location}:{bpn})
+      didDocumentBaseLocation: ""
+      # -- Base address of the IdentityHub Credential Service API
+      credentialServiceBaseAddress: ""
+      # -- DID of the IssuerService that issues the onboarding credentials
+      issuerDid: ""
+      # -- Issuer credential-definition ids for the onboarding credentials
+      bpnCredentialDefinitionId: ""
+      membershipCredentialDefinitionId: ""
+      # -- Super-user API key for the IdentityHub admin API. Secret-key 'identityhub-api-key'.
+      apiKey: ""
     bpnDidResolver:
       # -- ApiKey for management endpoint of the bpnDidResolver. Secret-key 'bpndidresolver-api-key'.
       apiKey: ""
